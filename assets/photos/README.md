# Haziqah photos

Drop 4 real photos here and name them:

- `photo-1.jpg`
- `photo-2.jpg`
- `photo-3.jpg`
- `photo-4.jpg`

Then update the `PHOTOS` list in `js/app.js` to use those `.jpg` files instead of the SVG placeholders.
